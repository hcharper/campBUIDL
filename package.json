{
  "name": "camp-buidl",
  "version": "1.0.0",
  "description": "ETH Denver Camp BUIDL - Token contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:101token": "hardhat run scripts/deploy101Token.js --network sepolia",
    "deploy:tokenshop": "hardhat run scripts/deployTokenShop.js --network sepolia"
  },
  "devDependencies": {
    "@chainlink/contracts": "^1.5.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.28.6"
  }
}
